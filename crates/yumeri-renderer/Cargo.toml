[package]
name = "yumeri-renderer"
version = "0.1.0"
edition = "2024"

[dependencies]
ash = "0.38.0"
ash-window = "0.13.0"
gpu-allocator = { version = "0.28.0", features = ["vulkan"] }
raw-window-handle = "0.6.2"
